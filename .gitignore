.DS_Store
# The assets directory is created by the build script.
# Assets are versioned separately.
assets
# Cotent images are computed from the assets.
content/photo/image/*.jpg
content/photo/image/*.png
